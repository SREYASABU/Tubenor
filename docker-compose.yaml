services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: tubenor-frontend
    hostname: frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    networks:
      - tubenor-network
    restart: unless-stopped

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: tubenor-backend
    hostname: backend
    env_file: "./backend/.env"
    ports:
      - "8000:8000"
    depends_on:
      tubenor-db:
        condition: service_healthy
    healthcheck:
      test: [ "CMD-SHELL", "curl -f http://localhost:8000/health || exit 1" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - tubenor-network
    restart: unless-stopped

  tubenor-db:
    image: postgres:17
    container_name: tubenor-db
    hostname: tubenor-db
    env_file: "backend/.env"
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      # - ./deployment/init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - tubenor-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5

    restart: unless-stopped
volumes:
  postgres_data:

networks:
  tubenor-network:
    driver: bridge